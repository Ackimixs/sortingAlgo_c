#include "sortAlgo.h"


int main() {

    //testingAllFunction(10000);

    runAll();

    /*int array[10000];

    int size = 10000;

    randomizeArray(array, size);

    //int array[10] = {5, 6, 7, 2, 9, 1, 4, 3, 8, 1};

    //int size = sizeof(array)/sizeof(array[0]);

    countingSort(array, size);

    printArray(array, size);

    printf("\n%d", isSorted(array, size));
*/
    return 0;
}
